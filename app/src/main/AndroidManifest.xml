<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xxskb.gtx" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".BaseApplication">
        <activity
            android:name=".view.MainActivity"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <intent-filter >
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable">
            </meta-data>
            <meta-data android:name="android.app.default_searchable" android:value=".view.MainActivity"></meta-data>
        </activity>
        <activity android:name=".view.TrainActivity">

        </activity>
        <provider
            android:authorities="com.xxskb.gtx.provider.SearchSuggestionProvider"
            android:name=".provider.SearchSuggestionProvider">
        </provider>
        <meta-data android:name="android.app.default_searchable" android:value=".view.MainActivity"></meta-data>
    </application>

</manifest>
